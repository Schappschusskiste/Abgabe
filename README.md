# Schnappschusskiste

Kurzanleitung:
Venv anlegen und aktivieren:
```shell
$ python -m venv .venv
$ . .venv/bin/activate
```
Dependencies installieren:
```shell
$ pip -Ir requirements.txt
```
Ausführen:
```shell
$ ./run.sh
```
